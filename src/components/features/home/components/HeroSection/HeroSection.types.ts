// components/features/home/components/HeroSection/HeroSection.types.ts
export interface HeroSectionProps {
    isDark: boolean;
}

export interface HeroBackgroundProps {
    isDark: boolean;
    children: React.ReactNode;
}

export interface HeroContentProps {
    isDark: boolean;
}
